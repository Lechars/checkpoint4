{% extends 'base.html.twig' %}

{% block title %}Artiste index{% endblock %}

{% block body %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}

    {{ message }}
{%endblock%}
