{% extends 'base.html.twig' %}

{% block title %}Artiste{% endblock %}

{% block body %}
    <h1>Artiste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artiste.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ artiste.Nom }}</td>
            </tr>
            <tr>
                <th>Carriere</th>
                <td>{{ artiste.Carriere }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td><img class="photo" src ="/assets/images/{{ artiste.photo }}" alt="photo du show"></td>
            </tr>
        </tbody>
    </table>


        <a href="{{ path('artiste_index') }}">back to list</a>

    {% if is_granted("ROLE_ADMIN") %}
        <a href="{{ path('artiste_edit', {'id': artiste.id}) }}">edit</a>
    {%endif%}

    {{ include('artiste/_delete_form.html.twig') }}
{% endblock %}
